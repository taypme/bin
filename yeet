#!/usr/bin/env bash
set -euo pipefail

if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
  echo "Not inside a git repository." >&2
  exit 1
fi

git add .
msg="yeet"

if [[ $# -gt 0 ]]; then
  msg="$*"
fi

git commit -m "$msg"
git push
